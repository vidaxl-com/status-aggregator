const expect = require('chai').expect

const statusGenerator = require('../../src')

describe('Unit Tests',  function () {

  // it('Testin malformed urls', async ()=>{
  //   expect(()=>statusGenerator.addApi('aaa')()).to.throw('The api is not a real url');
  // })

})
